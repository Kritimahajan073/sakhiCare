# Client (Next.js) — copy to .env in project root.
# For production, set these in Vercel (or your host) Environment Variables.

# Optional: GraphQL API URL (defaults to http://localhost:4000/graphql in dev)
# NEXT_PUBLIC_GRAPHQL_URI=http://localhost:4000/graphql
