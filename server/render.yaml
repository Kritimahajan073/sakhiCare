services:
  - type: web
    name: sakhicare-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    # Add MONGODB_URI and CLIENT_ORIGIN in Render dashboard
