# Server (Express) — copy to .env in the server/ directory.

# MongoDB (e.g. Atlas connection string)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/sakhicare?retryWrites=true&w=majority

# Port for the Express API (default 4000)
PORT=4000

# Optional: allowed CORS origin for the React client (default http://localhost:3000)
# CLIENT_ORIGIN=http://localhost:3000
